[project]
name = "coenvde-github-io"
version = "0"
description = "Blog static HTML build dependencies for GitHub Pages"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.5",
    "beautifulsoup4>=4.12",
    "pymdown-extensions>=10.7",
]

[tool.uv]
package = false
